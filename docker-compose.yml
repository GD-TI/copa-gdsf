services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "8080:5000"
    volumes:
      - ./index.html:/app/index.html:ro
    restart: unless-stopped
