services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - ./index.html:/app/index.html:ro
    restart: unless-stopped
